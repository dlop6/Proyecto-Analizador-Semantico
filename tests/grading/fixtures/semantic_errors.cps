// Debe informar CPS-119, CPS-120, CPS-215 y CPS-103 en una sola ejecucion.
function f(): integer {
  return 1;
}

class A {
  function m(): integer {
    return 1;
  }
}

let a: A = new A();
let producto = f * 2;
let metodo = a.m;
foreach (n in 1) {
  print(n);
}
print(noDeclarado);
